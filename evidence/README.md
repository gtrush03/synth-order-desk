# Evidence

JSON receipts copied from the source machine's `evidence/` folder after a secret scan, with machine paths replaced by `<run>`, `<control-room>`, `<forge>` and `<home>`. `self-check/` and `snyk/` hold this export's own verification output when the export tool ran them. Nothing here is edited by hand.
